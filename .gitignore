node_modules/
dist/
*.tsbuildinfo
.vite/
coverage/
*.log
.DS_Store
test-results/
playwright-report/
/e2e/.cache/
.playwright-mcp/
# Vitest Browser Mode failure artifacts (transient; not visual-regression baselines)
.vitest-attachments/
__screenshots__/

# Ignore only local Claude settings; skills + project guardrails (settings.json, rules/) stay tracked
.claude/settings.local.json
.claude/logs/
# Locally installed third-party skills (e.g. from plugins) must not get committed.
# The project's own skills live in plugins/event-storming-modeler/skills/ and are tracked there.
.claude/skills/
